# ClarosDPP — robots.txt
# The Docker build writes the configured public marketing origin below.

User-agent: *
Allow: /

# Block anything that shouldn't be indexed (none currently — all pages are public)
# Disallow: /private/

Sitemap: __MARKETING_URL__/sitemap.xml

# Internal Notes

This file exists because older repository memory notes were removed from top-level folders so that current documentation could live in one place.

For day-to-day engineering work, use the current docs outside `archive/`.
